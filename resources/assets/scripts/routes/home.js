// import { gsap } from 'gsap';

export default {
  init() {

    // Greensock animations ------------>

  //   $(window).on('load', function(){

  //     // Hands slide in from right
  //     gsap.to('.home-hero .hero-image', { duration: 1, x: -250, opacity: 1 });

  //     gsap.to('.animation__text-slide-up', { duration: .5, y: -100, opacity: 1, stagger: .25 });

  //   });
  },
  finalize() {
    // JavaScript to be fired on the home page, after the init JS
  },
};
